import pytest
import ts_filter
import transportpacket

STREAM_NAME = 'D:/Stream/Airtel/BizTrip(2016-08)/11640V_32700_20160901_partial.TRP'
class TestSectionFilter:
    def test_init(self):
        # section = filter.SectionFilter(0x0, 0x0)
        # assert isinstance(section, filter.SectionFilter)
        assert 1==1
